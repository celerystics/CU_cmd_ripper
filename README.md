# CU_cmd_ripper
an external command loader for casualties unknown. it loads custom commands from the console and adds them to the clipboard in a state that can just be pasted and used, while making @c cmds work as they would normally.
